projects:
  crn:
    schemaPath: src/graphql/schema.json
    includes:
      - src/graphql/**/*.ts
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: typescript
        generatedFileName: src/RequestAPI.ts
        docsFilePath: src/graphql
        maxDepth: 4
extensions:
  amplify:
    version: 3
